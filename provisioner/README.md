# Provisioner tool

This docker image serves to install terraform, and terragrunt.

Current versions:

Terraform: 0.12.26
Terragrunt: 0.23.24

## Build image

``` bash
docker build -t infra-provisioner .
```
